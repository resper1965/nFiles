# Supabase — Auth e Storage (n.files / ness)
# Copiar para .env.local e preencher com valores do dashboard Supabase.
# Nunca commitar .env.local ou chaves reais.

NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
# Chave pública (use uma das duas, conforme o dashboard Supabase):
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sua-publishable-key

# Opcional: só no servidor (Vercel env); nunca no cliente
# SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key

# Gemini (Google AI) — usar apenas no servidor (API routes); nunca no cliente
# GEMINI_API_KEY=sua-chave-gemini
